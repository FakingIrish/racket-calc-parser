Documentation:

Simply use the command line after running the parser.rkt program to scan/parse .txt file. 

(parse "Input01.txt) <--- example command line input


No references outside of docs.racket-lang.org were used. Racket provides quite an extensive library of functions and rules.
Unit testing and separate .rkt references are in the .zip.



It is time to sleep now.

-Job Dick
P.S. Many Bothans died to complete this project. 